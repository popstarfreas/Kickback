var Config = {
  enabled: true,
  dimensionKickbackName: "lobby",
  kickbackMessageEnabled: true,
  kickbackMessage: "The connection to the Server has ended. Connecting to the Lobby..",
  kickbackColor: "ff0000"
};

module.exports = Config;